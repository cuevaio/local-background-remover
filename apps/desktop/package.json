{
  "name": "desktop",
  "private": true,
  "license": "UNLICENSED",
  "main": "src/main/main.js",
  "scripts": {
    "dev": "electron .",
    "build": "node -e \"console.log('No desktop build pipeline configured yet')\"",
    "lint": "node -e \"console.log('No desktop lint configured yet')\"",
    "test": "node -e \"console.log('No desktop tests configured yet')\"",
    "check": "bun run lint && bun run test"
  },
  "devDependencies": {
    "electron": "^37.5.0"
  }
}
