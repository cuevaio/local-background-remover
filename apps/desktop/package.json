{
  "name": "desktop",
  "private": true,
  "license": "UNLICENSED",
  "main": "dist/main/main.js",
  "scripts": {
    "dev": "tsc -p tsconfig.json && node ./scripts/copy-renderer-assets.mjs && electron .",
    "build": "tsc -p tsconfig.json && node ./scripts/copy-renderer-assets.mjs",
    "lint": "node -e \"console.log('No desktop lint configured yet')\"",
    "test": "node -e \"console.log('No desktop tests configured yet')\"",
    "check": "bun run lint && bun run test"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "electron": "^41.1.0",
    "typescript": "^5.9.3"
  }
}
