# Plan: Release v0.5.1

## Goal
- Bump the desktop app and Python CLI release versions from `0.5.0` to `0.5.1`.
- Update the GitHub release workflow so the CLI build is the only required release artifact for now.
- Create the `v0.5.1` git tag and publish the matching GitHub release.

## Files To Update
- `.github/workflows/release.yml`
- `apps/desktop/package.json`
- `apps/rmbg/pyproject.toml`
- `apps/rmbg/uv.lock`

## Verification
1. Confirm the updated source versions read as `0.5.1`.
2. Run targeted build checks for the desktop app and CLI packaging metadata.
3. Commit the workflow change, version bump, and this plan file together.
4. Create tag `v0.5.1` from that commit and verify the CLI-only release publishes.
