# Plan: Clean CLI logs and prepare `v0.3.2`

## Goal
- Remove noisy internal/runtime logs from CLI model bootstrap and inference flows.
- Keep CLI JSON output user-friendly and deterministic for non-technical users.
- Publish the fix as `v0.3.2`.

## Actions
1. Handle multiprocessing internal resource-tracker entrypoints before argparse parsing.
2. Suppress Hugging Face download noise while keeping explicit stage progress output.
3. Suppress Transformers model-loading progress/log chatter during inference.
4. Add tests for resource-tracker argv detection and model download token/progress behavior.
5. Bump versioned CLI metadata to `0.3.2` and refresh lock metadata.

## Verification
1. `cd apps/rmbg && uv run python -m py_compile rmbg_cli/cli.py`
2. `cd apps/rmbg && uv run --group dev pytest tests/test_cli_license.py tests/test_model_manager.py tests/test_license_manager.py -q`
3. `cd apps/rmbg && uv run --group dev ruff check rmbg_cli tests`
4. `cd apps/rmbg && uv run --group dev pytest -q`
5. `cd apps/rmbg && uv run rmbg model ensure --model-dir "$(mktemp -d)" --surface cli --format json`
6. `cd apps/rmbg && uv run rmbg remove --input ../butterfly.jpg --output /tmp/image_rmbg_test.png --format json`
