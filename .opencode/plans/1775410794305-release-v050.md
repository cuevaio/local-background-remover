# Plan: Release v0.5.0

## Goal
- Bump the desktop app and Python CLI release versions from `0.4.4` to `0.5.0`.
- Keep release validation aligned with the existing GitHub workflow checks.
- Create the `v0.5.0` git tag and publish the matching GitHub release.

## Files To Update
- `apps/desktop/package.json`
- `apps/rmbg/pyproject.toml`
- `apps/rmbg/uv.lock`

## Verification
1. Confirm the updated source versions read as `0.5.0`.
2. Run targeted build checks for the desktop app and CLI packaging metadata.
3. Commit the version bump together with this plan file.
4. Create tag `v0.5.0` from that commit and publish the GitHub release.
